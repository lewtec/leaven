# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
# vim: set ts=2 sw=2 tw=0 fo=cnqoj
#
# Binary-only: archives + checksums. No Homebrew / Docker / nfpm.
# Targets match CI (.github/workflows/ci.yml).

version: 2
project_name: leaven

before:
  hooks:
    - go mod tidy

builds:
  - id: leaven
    binary: leaven
    main: ./cmd/leaven
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X github.com/lewtec/lewkit/x/release.version={{ .Version }}

archives:
  - formats: [tar.gz]
    # OS/Arch names compatible with uname-style results (same as gaderno/orvalho).
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: checksums.txt
  algorithm: sha256

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

snapshot:
  version_template: "{{ .Tag }}-next"

release:
  footer: >-

    ---

    Released by [GoReleaser](https://github.com/goreleaser/goreleaser).
